incident

# Session 23032026

## 2026-03-23T07:00:00+11:00 hourly run
- Runtime: ~00:18:00.
- Startup protocol completed:
  - reviewed the last 3 monitor notes first (`session-16032026.md`, `session-14032026.md`, `session-13032026.md`) and carried forward the open blockers as: runtime adoption on source `main`, recurring review-fix redispatch without an active session, and the need to inspect a real active lifecycle path before declaring recovery.
  - current source checkout is clean `main` at package version `0.42.0`; after `git fetch --prune`, `git status --short --branch` still shows `## main...origin/main`.
  - pinned startup checks completed with `--config-dir .bosun --repo-root .`: `node cli.mjs --daemon-status` initially reported `bosun daemon is not running`, and `node cli.mjs --where` resolved the active config root to repo-local `.bosun`.
- Runtime state and sink truth:
  - before recovery, repo-local sinks were stale at the final prior epoch: `.bosun/logs/monitor.log` last advanced at `2026-03-23 06:16:25 +11:00`, repo-local `.bosun/workflow-runs/index.json` at `06:16:26 +11:00`, and `_active-runs.json` still contained stale `running` entries for `Bosun PR Watchdog`, `GitHub ↔ Kanban Sync`, and `Task Batch Processor`.
  - after controlled recovery (`node cli.mjs --terminate` reported no live processes, then `node cli.mjs --daemon --config-dir .bosun --repo-root . --no-update-check --no-auto-update` started PID `71356`), the source daemon is running again and repo-local sinks resumed advancing.
  - fresh active sink paths after restart:
    - `.bosun/logs/monitor.log`
    - `.bosun/workflow-runs/index.json`
    - `.bosun/workflow-runs/_active-runs.json`
- Throughput target:
  - merged non-monitor PRs in the last hour: `0` (current time `2026-03-23T07:02:22+11:00`; latest merged PRs remain on `2026-03-22`, for example `#383` at `2026-03-22T16:38:06Z`).
  - `done=39`, which is up versus the last repo session note (`done=34` on `2026-03-16`), but last-hour throughput is still below target, so this run remains an incident.
- End-to-end deep inspection (required):
  1. source-down incident proof:
     - `node cli.mjs --daemon-status --config-dir .bosun --repo-root .` returned `bosun daemon is not running`.
     - `_active-runs.json` still held stale `running` entries (`de9f4c7d...`, `1e5a1905...`, multiple Task Batch runs), proving the prior runtime died mid-cycle rather than converging cleanly.
  2. active task path after restart:
     - fresh Task Batch run `4eb991e5-1d6f-45c0-9946-7fab9ab1c955` completed successfully on startup (`dispatch-tasks` no longer timed out in this epoch).
     - it dispatched Task Lifecycle run `89a094bf-79c0-4b07-89da-9e8668fbf6b2` for task `a3056106-c65c-422e-88b1-c7f46337df32` (`Repo map and architect-editor execution mode`).
     - that lifecycle path advanced through `claim-task -> set-inprogress -> acquire-worktree -> resolve-executor -> record-head`, and the run detail shows the current running node is `run-agent-plan`.
  3. active workflow regressions still present in the new epoch:
     - `GitHub ↔ Kanban Sync` run `e2ed8f31-4c3d-434e-ab25-3e4a4498256a` failed its deterministic `sync-programmatic` command on Windows and fell back to `sync-agent`; the run detail captures the truncated `node -e` parse error (`Expected ')', got '<eof>'`, `'token.startsWith' is not recognized...`).
     - review rehydrate immediately reproduced the older loop family: tasks `15266997...`, `6da44dbc...`, `bbe2f02e...`, and `a3056106...` logged `review REJECTED` followed by `supervisor dispatch-fix: no active session ... resetting task to todo`.
- Recent workflow behavior:
  - improved in this epoch:
    - startup `Task Batch Processor` completed cleanly instead of failing with `dispatch-tasks` timeout.
    - fresh `_active-runs.json` now reflects live runs rather than only the stale pre-crash entries.
  - still incorrect:
    - `GitHub ↔ Kanban Sync` deterministic path is broken on Windows shell quoting and is forcing fallback agent sync.
    - supervisor review-fix recovery still resets inreview tasks to `todo` when no active session is present, which is the same symptom family called out in older March monitor notes.
- Root cause chain:
  1. immediate incident:
     - symptom: Bosun was not running from the source repo, so there was zero last-hour throughput and stale `running` workflow artifacts.
     - proof: startup `--daemon-status` failed, while repo-local workflow sinks remained frozen at the prior epoch.
     - action: controlled source restart on clean `main` with explicit repo/config flags.
     - result: fresh Task Batch, Task Lifecycle, PR Watchdog, and Sync runs are now active again from repo-local sinks.
  2. remaining code-level blocker family:
     - symptom: review-fix redispatch still emits `dispatch-fix: no active session` on rehydrated inreview tasks, and GitHub sync still shells a broken `node -e` command on Windows.
     - proof:
       - fresh monitor log lines at `2026-03-22T20:05:20Z` through `20:05:23Z` show repeated `review REJECTED` -> `dispatch-fix: no active session`.
       - fresh sync run `e2ed8f31...` captures the exact `sync-programmatic` shell parse failure.
     - exact code paths to inspect first:
       - `infra/monitor.mjs` `dispatchFixTask` / `hasActiveSession` path around the `review-fix-redispatch` fallback.
       - `workflow-templates/github.mjs` `sync-programmatic` command builder around the inline `node -e` string; convert it to `command: "node"` with `args: ["-e", ...]` like `workflow-templates/task-batch.mjs` to avoid Windows shell truncation.
- Fix applied this run:
  - runtime recovery only; no source edits were made.
  - this restart is an operational recovery, not a durable code fix for the review-fix/session-linkage or sync-command quoting regressions.
- Validation performed:
  1. startup checklist commands: last 3 session notes, `git status --short --branch`, package version, `node cli.mjs --daemon-status`, `node cli.mjs --where`, `node cli.mjs --help`
  2. sink inspection: repo-local monitor log + workflow index + `_active-runs.json` mtimes and content
  3. task/workflow inspection: `task stats --json`, `task list --status inprogress --json`, task `9c7d7ce3...`, run details for `8ebe4c89...`, `de9f4c7d...`, `1e5a1905...`, `89a094bf...`, `4eb991e5...`, `e2ed8f31...`
  4. throughput check: `gh pr list --state merged ...`
  5. controlled source restart and post-restart verification
- Git / PR / CI status:
  - no code changes, no commit, no push, no PR this run.
- Local source freshness:
  - source checkout is clean `main` and matches `origin/main` after fetch.
- Narrow next-run handoff:
  1. inspect `workflow-templates/github.mjs` `sync-programmatic` first; the fresh repro is run `e2ed8f31-4c3d-434e-ab25-3e4a4498256a.json`.
  2. then inspect `infra/monitor.mjs` `dispatchFixTask` / `hasActiveSession` against the fresh `review REJECTED -> dispatch-fix: no active session` lines for tasks `15266997...`, `6da44dbc...`, `bbe2f02e...`, and `a3056106...`.
  3. do not reopen Task Batch timeout triage first unless a new startup batch run reintroduces `dispatch-tasks` timeout; the fresh startup batch run completed successfully in this epoch.

## 2026-03-23T08:11:49+11:00 hourly run
- Runtime: ~00:12:00.
- Startup protocol completed:
  - re-read the latest 3 monitor notes first and carried forward the open blockers as: review-fix redispatch with no active session, GitHub sync fallback noise, and last-hour throughput still below target.
  - verified source checkout before changes: `main...origin/main`, package `0.42.1`, source daemon healthy on PID `71356` with pinned `--config-dir .bosun --repo-root .`.
- Runtime state and sink truth:
  - active source process confirmed by command line: `C:\nvm4w\nodejs\node.exe ... C:\Users\jON\Documents\source\repos\virtengine-gh\bosun\cli.mjs --config-dir ...\.bosun --repo-root ...\bosun --no-update-check --no-auto-update --daemon-child`.
  - repo-local sinks are advancing and are authoritative for this epoch:
    - `.bosun/logs/monitor.log`
    - `.bosun/workflow-runs/index.json`
    - `.bosun/workflow-runs/_active-runs.json`
- Throughput target:
  - merged non-monitor PRs in the last hour remained `0`.
  - the apparent `done=39 / inreview=8 / inprogress=1` incident signal from pinned CLI commands was traced to stale workspace-mirror task-store reads, not the live repo-local store.
  - after the fix below, pinned `node cli.mjs task stats --json --config-dir .bosun --repo-root .` now reports repo-local truth: `done=55`, `inreview=1`, `inprogress=0`, `todo=29`, `draft=35`.
- End-to-end deep inspection (required):
  1. traced active lifecycle run `0070429a-9770-4796-b4f3-2116adb40574` end to end.
     - run artifact says task `c8067759-906a-4952-a59a-44d1a69c8807` (`Sift-style semantic command router and context budget policies`) is a resumed lifecycle retry currently marked at `run-agent-tests`, while the issue-advisor summary still says `Resume from Agent Implement.`
     - repo-local task store already had `c8067759...` back in `todo` at `2026-03-22T21:00:55Z`, proving the stale CLI counters were not reflecting the repo-local runtime truth.
  2. traced review occupancy against GitHub state.
     - workspace-mirror task store still showed tasks `b4db2d76...`, `4bca3be7...`, `6ed0db8f...`, `939cec8f...`, and `ad9d4563...` as `inreview`.
     - repo-local task store already had those tasks reconciled to `done`.
     - GitHub confirms PRs `#357`, `#358`, `#359`, `#360`, and `#361` are `MERGED`, so the stale workspace mirror was directly undercounting completion and overstating review backlog.
  3. confirmed the remaining real blocker family still reproduces in repo-local logs.
     - at `2026-03-22T20:05:20Z` through `20:05:23Z`, monitor logs show `review REJECTED` followed by `supervisor dispatch-fix: no active session` for tasks `15266997...`, `6da44dbc...`, `bbe2f02e...`, and `a3056106...`.
     - repo-local truth now narrows the active review queue to task `a3056106...` / PR `#394`, which is still open with failing checks and `reviewStatus=changes_requested`.
- Root cause chain:
  1. symptom: pinned CLI/monitor health checks were reading a stale workspace-mirror kanban store, making Bosun appear stuck at `done=39`, `inreview=8`, `inprogress=1` even though the repo-local daemon had already reconciled merged review tasks to `done`.
  2. proof:
     - repo-local `.bosun/.cache/kanban-state.json` = `120` tasks with `done=55`, `inreview=1`, `inprogress=0`.
     - workspace-mirror `.bosun/workspaces/virtengine-gh/bosun/.bosun/.cache/kanban-state.json` = `97` tasks with stale `done=39`, `inreview=8`, `inprogress=1`.
     - `task/task-cli.mjs` resolved the active workspace store before honoring the explicit `REPO_ROOT` passed via `node cli.mjs ... --repo-root .`.
  3. fixed code path this run:
     - `task/task-cli.mjs` `resolveKanbanStorePath()` now prefers explicit `REPO_ROOT` before the workspace-mirror lookup.
     - `tests/task-cli.test.mjs` adds a regression proving explicit repo-root wins over the active workspace mirror.
- Fix applied:
  - shipped clean branch/worktree fix from `C:\tmp\bosun-monitor-taskcli-fix`.
  - commit: `a56d45b9` (`fix(task-cli): honor explicit repo-root task store`).
  - branch: `monitor/bosun-env-stability`.
  - PR: `#396` `fix: honor explicit repo-root task store`.
  - labels applied: `codex`, `codex-automation`.
- Validation performed:
  1. `npm test -- tests/task-cli.test.mjs`
  2. `npm test`
  3. `npm run build`
  4. `npm run prepush:check`
  - additionally rechecked the fixed live behavior from both the source repo and the clean worktree with pinned `node cli.mjs task stats --json --config-dir ... --repo-root ...`, both now resolving the repo-local store (`done=55`, `inreview=1`, `inprogress=0`).
- Git / PR / CI status:
  - source repo restored to `main...origin/main` after isolating the patch into the temp worktree; no source-branch switch was left behind.
  - PR `#396` is open and initial CI is running (`Build + Tests`, CodeQL, and `📋 Template Integrity` in progress at close).
- Local source freshness:
  - yes, the source checkout remains on latest merged `main` (`main...origin/main`).
- Remaining true blocker / narrow next-run handoff:
  1. start in `infra/monitor.mjs` review-fix redispatch path around `dispatchFixTask` / `hasActiveSession`.
  2. use repo-local task `a3056106-c65c-422e-88b1-c7f46337df32` and PR `#394` as the concrete repro: repo-local store shows it as the only live `inreview` task, `reviewStatus=changes_requested`, and logs show `dispatch-fix: no active session` immediately after review rejection.
  3. do not reopen task-store drift first unless pinned `task stats --config-dir .bosun --repo-root .` regresses away from the repo-local `done=55 / inreview=1 / inprogress=0` state.
## 2026-03-23T09:15:00+11:00 hourly run
- Runtime: ~00:53:00.
- Startup protocol completed:
  - re-read the latest 3 monitor notes and carried forward the open blocker list as: stale task-cli workspace-mirror reads, the need to confirm a real active lifecycle path, and CI/PR throughput proof before calling the system healthy.
  - verified the live source checkout first: branch `main`, package `0.42.2`, `git fetch --prune` + `git rev-list --left-right --count origin/main...HEAD` => `0 0`; pre-existing local edits remain in workflow/agent/workspace files and were treated as unrelated evidence, not reverted.
  - pinned startup checks completed with `--config-dir .bosun --repo-root .`: source daemon still running on PID `71356`, `node cli.mjs --where` still resolves repo-local `.bosun`, and the live daemon command line still points to source `cli.mjs` with explicit repo/config flags.
- Runtime state and active sink paths:
  - authoritative advancing sinks remain repo-local:
    - `.bosun/logs/monitor.log`
    - `.bosun/workflow-runs/index.json`
    - `.bosun/workflow-runs/_active-runs.json`
  - at probe time, `_active-runs.json` contained live PR Watchdog run `6967efb8-8294-4e70-b7f0-96addfe6ba4a` and Task Lifecycle run `d64a89a3-d206-425f-a5ad-1ac986ea46d4`.
- End-to-end deep inspection (required):
  1. traced the live Task Lifecycle path via run `d64a89a3-d206-425f-a5ad-1ac986ea46d4`.
     - current task/run proof: `claim-task -> set-inprogress -> acquire-worktree -> resolve-executor -> record-head -> read-workflow-contract -> workflow-contract-validation -> build-prompt` all completed.
     - current running node is `run-agent-plan`.
     - run artifact shows the actual live task is `0bc4b6cd-cf09-46a9-9777-dc6ed13e7b7e`, not the stale mirror task surfaced by the pinned CLI.
  2. traced current PR Watchdog behavior via run `6967efb8-8294-4e70-b7f0-96addfe6ba4a`.
     - fetch/classify completed, `programmatic-fix` identified active PR work, and fallback `dispatch-fix-agent` is still running rather than silently no-oping.
  3. proved the recurring monitor-side blocker from the prior session is the task CLI path, not the GitHub sync path.
     - live monitor log now shows repeated `sync-programmatic` `node:start` -> `node:complete` success through the current epoch.
     - stale `review REJECTED -> dispatch-fix: no active session` lines remain historical evidence from `2026-03-22T20:05Z`, but the fresh incident this run was that pinned CLI health checks still loaded the wrong store.
- Root cause chain:
  1. symptom: pinned `node cli.mjs task stats --json --config-dir .bosun --repo-root .` still returned the stale 97-task workspace-mirror snapshot (`done=39`, `inreview=8`, `blocked=13`) instead of the repo-local source-of-truth store.
  2. proof:
     - current source code in `task/task-cli.mjs` `resolveKanbanStorePath()` still prioritized the active workspace mirror after `BOSUN_STORE_PATH` and ignored explicit `REPO_ROOT`.
     - repo-local `.bosun/.cache/kanban-state.json` currently holds `120` tasks (`done=56`, `todo=27`, `inreview=1` at first probe; later `todo=28`, `inreview=0`), while the workspace mirror still holds the stale `97`-task snapshot with task `9c7d7ce3...` frozen `inprogress` at `2026-03-22T01:02:33.083Z`.
  3. responsible code path: `task/task-cli.mjs` `resolveKanbanStorePath()`.
- Fix applied:
  - shipped a clean main-based source fix from `C:\tmp\bosun-monitor-taskcli-fix`:
    - `task/task-cli.mjs`: explicit `REPO_ROOT` now short-circuits store resolution before any active-workspace mirror lookup.
    - `tests/task-cli.test.mjs`: added a regression proving explicit `REPO_ROOT` beats the workspace mirror path.
    - `package.json` + `package-lock.json`: bumped version to `0.42.3`.
  - commit: `8cfe8715` (`fix(task-cli): prefer explicit repo-root store`).
  - pushed to `monitor/bosun-env-stability`.
  - opened PR `#398` `fix: prefer explicit repo-root task store` and applied labels `codex` + `codex-automation`.
- Validation performed:
  1. `npm test -- tests/task-cli.test.mjs`
  2. `npm test`
  3. `npm run build`
  4. `npm run prepush:check`
  - post-fix runtime verification from the clean worktree against the live source repo:
    - `node cli.mjs task stats --json --config-dir C:\Users\jON\Documents\source\repos\virtengine-gh\bosun\.bosun --repo-root C:\Users\jON\Documents\source\repos\virtengine-gh\bosun`
    - result now loads only the repo-local store (`120` tasks; `done=56`, `todo=28`, `inprogress=1`, `inreview=0`).
- Throughput target:
  - last-hour merged non-monitor PRs: `1` (`#394` merged `2026-03-22T21:18:46Z`).
  - real work is still progressing live, but the stricter 2-3 completed-tasks/hour target is not yet met, so this run stays at `warning` rather than `healthy`.
- Recent workflow behavior:
  - source daemon/runtime remain healthy and advancing from repo-local sinks.
  - current active lifecycle path is real and observable.
  - GitHub sync is behaving correctly in the current epoch.
  - the task-cli mirror/readback drift from the previous session is now fixed in source and awaiting CI/merge.
- Git / PR / CI status:
  - source repo remains on latest merged `main` (`0 0` vs `origin/main`).
  - PR `#398` is open; initial checks are in progress (`Build + Tests`, CodeQL, `📋 Template Integrity`), while `Attach Bosun marker` and `Codacy Security Scan` already passed.
- Narrow next-run handoff:
  1. re-check PR `#398` CI first; if green, merge it and restart the source daemon onto merged `main` so monitor health checks use the corrected task store path.
  2. after merge/restart, re-run pinned `task stats --config-dir .bosun --repo-root .` in the source repo and confirm it still reports the repo-local 120-task store.
  3. only after that, revisit the older `dispatch-fix: no active session` family if it still reproduces in the new epoch.

## 2026-03-23T12:10:00+11:00 hourly run
- Runtime: ~00:10:00.
- Startup protocol completed:
  - re-read the latest 3 monitor notes first and carried forward the open blockers as: the unadopted task-cli repo-root fix, the need to verify a real live lifecycle path, and last-hour throughput below target.
  - verified live source checkout first: branch `main`, `git rev-list --left-right --count origin/main...HEAD => 0 0`, package `0.42.2`, with pre-existing unrelated edits in workflow/agent/server/workspace files preserved and not reverted.
  - pinned startup checks completed with `--config-dir .bosun --repo-root .`: source daemon healthy on PID `71940`, `node cli.mjs --where` still resolves repo-local `.bosun`, and command surface was rechecked with `node cli.mjs --help`.
- Runtime state and active sink paths:
  - authoritative advancing sinks remain repo-local:
    - `.bosun/logs/monitor.log`
    - `.bosun/workflow-runs/index.json`
    - `.bosun/workflow-runs/_active-runs.json`
  - probe-time mtimes were fresh in the current epoch (`12:01:43` to `12:01:50` local).
- Throughput target:
  - merged non-monitor PRs in the last hour: `0` (`gh pr list --state merged --search "base:main merged:>=2026-03-23T00:10:07Z"` returned `[]`).
  - because the last-hour merge count is `0`, this session remains an incident even though real workflows are active.
- End-to-end deep inspection (required):
  1. traced the live Task Lifecycle run [`0bf85684-fcae-4153-a08c-fc94a3f297d7.json`](C:/Users/jON/Documents/source/repos/virtengine-gh/bosun/.bosun/workflow-runs/0bf85684-fcae-4153-a08c-fc94a3f297d7.json).
     - current task: `e7b6f2bb-c629-4939-a3a5-13074fd51b9c` (`[l] feat(kanban): propagate PR linkage integrity from status updates through board refresh`)
     - current node: `run-agent-plan`
     - claim owner: `wf-8a91dc10`
     - claim token: `7767dfb9-d2cd-428d-8fb5-5c0520f93f0f`
     - resolved executor: `codex` / `gpt-5.4` / profile `microsoft-hve-core-implementation-validator`
     - worktree path: `C:\Users\jON\Documents\source\repos\virtengine-gh\bosun\.bosun\worktrees\task-e7b6f2bbc629-d1877d5d4c`
     - repo-local task state for that task is `inprogress`, `prNumber=null`, `reviewStatus=null`, repo areas `kanban,ui/components,task`.
  2. traced recent Task Lifecycle failure immediately before the active run:
     - monitor log shows task `c0145edf-8f13-47d3-a259-7486ea23f2c1` advanced through `run-agent-implement -> pre-pr-validation`, then failed validation and returned `inprogress -> todo` at `2026-03-23T01:00:33Z`.
  3. traced current PR Watchdog behavior:
     - live monitor log shows `programmatic-fix` completed successfully and the fresh cycle reached `review-needed -> programmatic-review`, so the older `dispatch-fix: no active session` family is not the primary blocker in this epoch.
- Recent workflow behavior:
  - `Bosun PR Watchdog` is behaving correctly in the current epoch.
  - `Task Lifecycle` is actively progressing a real repo-local task path.
  - `Task Batch Processor` is still functionally wrong for monitor decisions: the current log window shows repeated `Task batch completed: 0/0 succeeded (0 failed)` despite the repo-local store having active backlog work.
- Root cause chain:
  1. symptom:
     - pinned monitor CLI reads the wrong task store and reports stale backlog health while the live repo-local runtime is working different tasks.
  2. proof:
     - source `0.42.2` `node cli.mjs task stats --json --config-dir .bosun --repo-root .` still prints `[task-store] Loaded 120 tasks from disk` followed by `[task-store] Loaded 97 tasks from disk`, then reports the stale `97`-task mirror snapshot (`done=44`, `inreview=3`, `blocked=13`).
     - the only `inprogress` task surfaced by the source CLI is stale task `9c7d7ce3...`, while the live repo-local workflow run is on task `e7b6f2bb...`.
     - current source `task/task-cli.mjs` still resolves the active workspace mirror before honoring explicit `REPO_ROOT`, which matches the live double-load repro.
     - the validated fixed worktree at `C:\tmp\bosun-monitor-taskcli-readopt` running against the same source repo/config now loads only the repo-local store (`120` tasks; `done=56`, `todo=18`, `inprogress=1`, `inreview=4`, `blocked=6`).
  3. responsible code path:
     - [`task/task-cli.mjs`](C:/Users/jON/Documents/source/repos/virtengine-gh/bosun/task/task-cli.mjs) `resolveKanbanStorePath()`.
- Fix applied:
  - replayed the prior unmerged fix commit `8cfe8715` onto clean worktree [`C:\tmp\bosun-monitor-taskcli-readopt`](C:/tmp/bosun-monitor-taskcli-readopt) as commit `e743ead6` (`fix(task-cli): prefer explicit repo-root store`).
  - branch pushed: `monitor/bosun-env-stability`.
  - opened replacement PR [`#403`](https://github.com/virtengine/bosun/pull/403) `fix: prefer explicit repo-root task store`.
  - labels applied: `codex`, `codex-automation`.
  - this is the durable source fix for the current incident; no runtime cleanup or task reset was used.
- Validation performed:
  1. `npm test -- tests/task-cli.test.mjs`
  2. `npm test`
  3. `npm run build`
  4. `npm run prepush:check`
  - all four commands passed in the clean worktree at `0.42.3`.
  - additional live verification from the fixed worktree against the source repo:
    - `node cli.mjs task stats --json --config-dir C:\Users\jON\Documents\source\repos\virtengine-gh\bosun\.bosun --repo-root C:\Users\jON\Documents\source\repos\virtengine-gh\bosun`
    - result: `[task-store] Loaded 120 tasks from disk` and repo-local totals only.
- Git / PR / CI status:
  - source checkout remains on latest merged `main` (`0 0` vs `origin/main`) but does not yet include the fix because PR `#403` is still open.
  - initial CI watch for `#403`:
    - passed: `Attach Bosun marker`, `Codacy Security Scan`, `📋 Template Integrity`, `CodeQL Analyze (actions)`
    - still running at close: `Build + Tests`, two `CodeQL Analyze (javascript-typescript)` jobs, sandbox shards, flakiness detection, and existing E2E suite
- Local source freshness:
  - yes, the source repo still reflects the latest merged `main`.
  - no daemon restart was performed this run because the fix is not merged yet; restart the source daemon onto merged `main` only after `#403` lands.
- Narrow next-run handoff:
  1. start with PR `#403`; if CI is green, merge it and restart the source daemon with `node cli.mjs --terminate && node cli.mjs --daemon --config-dir .bosun --repo-root . --no-update-check --no-auto-update`.
  2. immediately re-run pinned `task stats --config-dir .bosun --repo-root .` in the source repo and confirm it now reports only the repo-local `120`-task store.
  3. only after that, reassess Task Batch throughput and whether any older review-fix/session drift still reproduces in the new epoch.
