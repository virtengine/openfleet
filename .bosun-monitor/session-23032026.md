incident

# Session 23032026

## 2026-03-23T07:00:00+11:00 hourly run
- Runtime: ~00:18:00.
- Startup protocol completed:
  - reviewed the last 3 monitor notes first (`session-16032026.md`, `session-14032026.md`, `session-13032026.md`) and carried forward the open blockers as: runtime adoption on source `main`, recurring review-fix redispatch without an active session, and the need to inspect a real active lifecycle path before declaring recovery.
  - current source checkout is clean `main` at package version `0.42.0`; after `git fetch --prune`, `git status --short --branch` still shows `## main...origin/main`.
  - pinned startup checks completed with `--config-dir .bosun --repo-root .`: `node cli.mjs --daemon-status` initially reported `bosun daemon is not running`, and `node cli.mjs --where` resolved the active config root to repo-local `.bosun`.
- Runtime state and sink truth:
  - before recovery, repo-local sinks were stale at the final prior epoch: `.bosun/logs/monitor.log` last advanced at `2026-03-23 06:16:25 +11:00`, repo-local `.bosun/workflow-runs/index.json` at `06:16:26 +11:00`, and `_active-runs.json` still contained stale `running` entries for `Bosun PR Watchdog`, `GitHub ↔ Kanban Sync`, and `Task Batch Processor`.
  - after controlled recovery (`node cli.mjs --terminate` reported no live processes, then `node cli.mjs --daemon --config-dir .bosun --repo-root . --no-update-check --no-auto-update` started PID `71356`), the source daemon is running again and repo-local sinks resumed advancing.
  - fresh active sink paths after restart:
    - `.bosun/logs/monitor.log`
    - `.bosun/workflow-runs/index.json`
    - `.bosun/workflow-runs/_active-runs.json`
- Throughput target:
  - merged non-monitor PRs in the last hour: `0` (current time `2026-03-23T07:02:22+11:00`; latest merged PRs remain on `2026-03-22`, for example `#383` at `2026-03-22T16:38:06Z`).
  - `done=39`, which is up versus the last repo session note (`done=34` on `2026-03-16`), but last-hour throughput is still below target, so this run remains an incident.
- End-to-end deep inspection (required):
  1. source-down incident proof:
     - `node cli.mjs --daemon-status --config-dir .bosun --repo-root .` returned `bosun daemon is not running`.
     - `_active-runs.json` still held stale `running` entries (`de9f4c7d...`, `1e5a1905...`, multiple Task Batch runs), proving the prior runtime died mid-cycle rather than converging cleanly.
  2. active task path after restart:
     - fresh Task Batch run `4eb991e5-1d6f-45c0-9946-7fab9ab1c955` completed successfully on startup (`dispatch-tasks` no longer timed out in this epoch).
     - it dispatched Task Lifecycle run `89a094bf-79c0-4b07-89da-9e8668fbf6b2` for task `a3056106-c65c-422e-88b1-c7f46337df32` (`Repo map and architect-editor execution mode`).
     - that lifecycle path advanced through `claim-task -> set-inprogress -> acquire-worktree -> resolve-executor -> record-head`, and the run detail shows the current running node is `run-agent-plan`.
  3. active workflow regressions still present in the new epoch:
     - `GitHub ↔ Kanban Sync` run `e2ed8f31-4c3d-434e-ab25-3e4a4498256a` failed its deterministic `sync-programmatic` command on Windows and fell back to `sync-agent`; the run detail captures the truncated `node -e` parse error (`Expected ')', got '<eof>'`, `'token.startsWith' is not recognized...`).
     - review rehydrate immediately reproduced the older loop family: tasks `15266997...`, `6da44dbc...`, `bbe2f02e...`, and `a3056106...` logged `review REJECTED` followed by `supervisor dispatch-fix: no active session ... resetting task to todo`.
- Recent workflow behavior:
  - improved in this epoch:
    - startup `Task Batch Processor` completed cleanly instead of failing with `dispatch-tasks` timeout.
    - fresh `_active-runs.json` now reflects live runs rather than only the stale pre-crash entries.
  - still incorrect:
    - `GitHub ↔ Kanban Sync` deterministic path is broken on Windows shell quoting and is forcing fallback agent sync.
    - supervisor review-fix recovery still resets inreview tasks to `todo` when no active session is present, which is the same symptom family called out in older March monitor notes.
- Root cause chain:
  1. immediate incident:
     - symptom: Bosun was not running from the source repo, so there was zero last-hour throughput and stale `running` workflow artifacts.
     - proof: startup `--daemon-status` failed, while repo-local workflow sinks remained frozen at the prior epoch.
     - action: controlled source restart on clean `main` with explicit repo/config flags.
     - result: fresh Task Batch, Task Lifecycle, PR Watchdog, and Sync runs are now active again from repo-local sinks.
  2. remaining code-level blocker family:
     - symptom: review-fix redispatch still emits `dispatch-fix: no active session` on rehydrated inreview tasks, and GitHub sync still shells a broken `node -e` command on Windows.
     - proof:
       - fresh monitor log lines at `2026-03-22T20:05:20Z` through `20:05:23Z` show repeated `review REJECTED` -> `dispatch-fix: no active session`.
       - fresh sync run `e2ed8f31...` captures the exact `sync-programmatic` shell parse failure.
     - exact code paths to inspect first:
       - `infra/monitor.mjs` `dispatchFixTask` / `hasActiveSession` path around the `review-fix-redispatch` fallback.
       - `workflow-templates/github.mjs` `sync-programmatic` command builder around the inline `node -e` string; convert it to `command: "node"` with `args: ["-e", ...]` like `workflow-templates/task-batch.mjs` to avoid Windows shell truncation.
- Fix applied this run:
  - runtime recovery only; no source edits were made.
  - this restart is an operational recovery, not a durable code fix for the review-fix/session-linkage or sync-command quoting regressions.
- Validation performed:
  1. startup checklist commands: last 3 session notes, `git status --short --branch`, package version, `node cli.mjs --daemon-status`, `node cli.mjs --where`, `node cli.mjs --help`
  2. sink inspection: repo-local monitor log + workflow index + `_active-runs.json` mtimes and content
  3. task/workflow inspection: `task stats --json`, `task list --status inprogress --json`, task `9c7d7ce3...`, run details for `8ebe4c89...`, `de9f4c7d...`, `1e5a1905...`, `89a094bf...`, `4eb991e5...`, `e2ed8f31...`
  4. throughput check: `gh pr list --state merged ...`
  5. controlled source restart and post-restart verification
- Git / PR / CI status:
  - no code changes, no commit, no push, no PR this run.
- Local source freshness:
  - source checkout is clean `main` and matches `origin/main` after fetch.
- Narrow next-run handoff:
  1. inspect `workflow-templates/github.mjs` `sync-programmatic` first; the fresh repro is run `e2ed8f31-4c3d-434e-ab25-3e4a4498256a.json`.
  2. then inspect `infra/monitor.mjs` `dispatchFixTask` / `hasActiveSession` against the fresh `review REJECTED -> dispatch-fix: no active session` lines for tasks `15266997...`, `6da44dbc...`, `bbe2f02e...`, and `a3056106...`.
  3. do not reopen Task Batch timeout triage first unless a new startup batch run reintroduces `dispatch-tasks` timeout; the fresh startup batch run completed successfully in this epoch.
