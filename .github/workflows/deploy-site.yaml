# Deploy OpenFleet landing page to GitHub Pages
#
# Runs in the virtengine/openfleet repo (or triggered via submodule push).
# No cross-repo token needed — uses the built-in GITHUB_TOKEN.
#
# GitHub Pages settings for virtengine/openfleet:
#   Settings → Pages → Source: GitHub Actions
#
# The CNAME file in site/ keeps openfleet.virtengine.com wired up.

name: "OpenFleet: Deploy Site"

on:
  push:
    branches: [main]
    paths:
      - "site/**"
  workflow_dispatch:

jobs:
  deploy:
    name: Deploy to GitHub Pages
    runs-on: ubuntu-latest
    permissions:
      contents: write
      pages: write
      id-token: write

    steps:
      - name: Checkout
        uses: actions/checkout@v4

      - name: Deploy to gh-pages
        uses: peaceiris/actions-gh-pages@v4
        with:
          github_token: ${{ secrets.GITHUB_TOKEN }}
          publish_branch: gh-pages
          publish_dir: site
          cname: openfleet.virtengine.com
          commit_message: "chore(site): deploy ${{ github.sha }}"
          force_orphan: false
